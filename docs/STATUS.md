# Status — as of 2026-08-09

Working state and open threads. Companion to [\_AI_AGENT_PRIMER.md](../_AI_AGENT_PRIMER.md), which is the stable reference; this file is the volatile one. Update it at the end of each session.

## Where things stand

Branch `feat/logo`, working tree clean, HEAD `c0ebc9d`. Two workstreams ran in parallel and both are at a natural stopping point.

**Site and SEO groundwork — done and committed, not deployed.** The holding page, nginx config, webroot restructure, GA4, canonical host and crawl setup are all finished. Nothing has been pushed to the NAS this session.

**Logo — active. Lowercase direction, study 003, presented to the client.** The uppercase direction is abandoned; see the primer §8 for why the metric system makes lowercase work.

## Deploy state

The site has **not** been deployed since this work landed. Two things block a clean run:

- `prod.sh` gates on branch `main` ([prod.sh:98](../utils/deploy/prod.sh#L98)) and the work is on `feat/logo`. Merge first, or pass `--force`.
- Public reachability also needs the Cloudflare Tunnel route, which is a dashboard action, not something `prod.sh` does. Check [nas-setup.md §3](nas-setup.md) before assuming the deploy is what is broken if the URL 404s.

Whether the tunnel route has ever been added is **unverified**. Confirm with `curl -sS -o /dev/null -w '%{http_code}\n' https://motoproponent.com/` before debugging anything else — `1033` or a bare 404 means the route is missing, `502` means the route exists but the container is not answering.

## Logo — the state of play

Latest: `assets/logos/motoproponent-lowercase-study-003.svg`, from `motoproponent-lowercase-generator.py`.

Study 003 applied four changes agreed with Ziad:

1. **50/50 glyph optimisation.** Studies 000–002 drew each glyph to read its best right way up, which produced a crisp `motopro` followed by mush. For an object read from both sides of a helmet that means a good side and a bad side. Each glyph now sits nearer the middle, so the whole word is evenly semi-legible.
2. **Heavier stroke** — `W_MAIN` 15 → 22. Paint, vinyl, embroidery and a 32px favicon all want mass.
3. **The rules are owned by a letter.** They were free-standing horizontals, which is why they caged the mark. The `m` now has a foot bar and that bar is what extends; rotated, it is the final `t`'s crossbar. They also stop past the pivot rather than closing a box.
4. **Tier two: the pivot-`o` roundel.** Thirteen symmetric letters is roughly 13:1 — fine as a helmet stripe, useless as an avatar or favicon. The roundel still reads at 32px.

### Open design decisions

- **Is the r/p compromise acceptable?** That pair is geometrically impossible (handedness is not preserved by 180° rotation) and is currently drawn to favour the `r`. Ziad has not signed off.
- **Heavier or lighter variant of 003?** Offered, not yet requested.
- **Mitre spikes at the `m`'s apex** — carried over from the uppercase studies, never resolved. Probably moot now.

### Correction on the record

An earlier draft claimed lowercase *solved* the o↔n pair. It does not — the notch width has no winning value (15px closes optically and the `n` reverts to an `o`; 34px turns the `o` into a `u` and the half read `mutupru`). It is a compromise at 20px, helped more by giving the bowl straight sides than by tuning the notch. The generator docstring and the study sheet notes were corrected; do not reintroduce the stronger claim.

## Client presentation page

Published as a **private** Claude artifact: <https://claude.ai/code/artifact/72640493-4617-411f-ab88-5cf0c4420f19>

Generated by `assets/logos/build-presentation.py` → `assets/logos/presentation.html` (333KB; the client's sketch is inlined as a base64 JPEG). It lifts artwork straight out of the study sheets rather than redrawing it, so it cannot drift. Re-run the builder after any study change, then **republish the same file path** to keep that URL.

Written in plain language at Ziad's instruction — Jeffrey is not a designer, so the copy explains the geometry in terms of bites out of letters, not counters and handedness.

**Two things to settle before it goes to Jeffrey:**

- The page's closing section asks four questions, two of which Ziad has not approved: whether the red is right, and whether Jeffrey wants a helmet mockup. Edit or cut them.
- It is private until shared from the artifact page's share menu.

## Waiting on Jeffrey

Six questions in `docs/.private/public-information.md` (git-ignored). Answers so far are recorded in `docs/.private/brand-decisions.md`.

Question 4 carries a pending default rather than an answer — "using motoproponent@gmail.com for anything public unless you say otherwise". Ziad was offered the option to move it to the answered file and did not take it, so it stays in the questions doc.

Also outstanding from Jeffrey's side: add `motoproponent.com` as the **first** link in YouTube Studio → Customization → Basic info → Links, and confirm the channel description mentions the domain.

## Time-sensitive

**Verify Google Search Console as a Domain property.** GSC retains only 16 months of data, and only from the date of verification — that history cannot be backfilled. Verify via a Cloudflare DNS `TXT` record at the apex, DNS-only (TXT cannot be proxied), then one-click import into Bing Webmaster Tools. Leave the TXT record in place forever; deleting it un-verifies the property.

Not yet done. Every day of delay is a day of history permanently lost.

## Known stale in-repo

Flagged in full in the primer §10. The short version: `assets/logos/README.md` and the "The ambigram" section of the root `README.md` both still describe the abandoned uppercase direction, and `motoproponent-duotone-generator.py` was left mid-edit and never re-run. None of it blocks anything; all of it will mislead an agent that trusts it.

## Suggested next actions

1. Confirm whether the tunnel route exists (`curl` the public URL). One command, and it determines whether "deploy" means anything yet.
2. Verify GSC as a Domain property. Time-sensitive, unrelated to everything else, roughly ten minutes.
3. Settle the r/p question, then decide whether 003 is the mark or whether a weight variant is worth a pass.
4. Trim the two unapproved questions off the presentation page and send it to Jeffrey.
5. Rewrite `assets/logos/README.md` for the lowercase direction, or add a header marking it superseded.
6. Merge `feat/logo` → `main` so `prod.sh` runs without `--force`.
